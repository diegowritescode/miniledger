# Deployment

## Environments
Local, staging, production — what differs.

## Pipeline
GitHub Actions: lint → test → build → (deploy).

## Runtime
Docker images, Nginx, SSL, host (VPS/AWS). Live URL.

## Configuration
Environment variables (see `.env.example`) and how they're injected.

## Rollback & observability
How to roll back; logs, metrics, health checks.
