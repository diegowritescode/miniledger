
# --- portfolio template rules ---
node_modules/
dist/
build/
coverage/
.nyc_output/
.env
.env.*
!.env.example
*.log
.DS_Store
.idea/
.vscode/

# local demo transcript (curated version lives in docs/demo.md)
miniledger-demo.txt

# mutation testing (Stryker) — sandbox may contain a copied .env, never commit it
.stryker-tmp/
reports/mutation/
